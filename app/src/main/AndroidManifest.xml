<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.example.marius.helpmesee">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".mainscreen.MainScreenPresenter">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

    <activity android:name=".directions.DirectionsScreenPresenter">

    </activity>

    <activity android:name=".location.LocationScreenPresenter">

    </activity>

    <activity android:name=".scene_description.SceneDescPresenter">

    </activity>

    <activity android:name=".text_recognition.TextRecPresenter">

    </activity>


  </application>

</manifest>